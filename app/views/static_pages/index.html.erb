<% if current_user  %>
<div class="google">Welcome, you are now signed in as <%= "#{current_user.first_name} #{current_user.last_name}" %>
  <%= link_to "Logout", logout_path %></p>
    <% else %>
    <p><%= link_to "Sign in with Google", "/auth/google" %></p>
    <% end %>
   </div>
<div id='app'>
</div>
  
